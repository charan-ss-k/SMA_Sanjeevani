fastapi
uvicorn[standard]
requests
pydantic
pydantic-settings>=2.0.0  # For settings management
python-multipart

# TTS - Multi-language support with Indic Parler-TTS and fallbacks
gtts>=2.5.0
pydub>=0.25.1
google-cloud-texttospeech>=2.16.0
# Indic Parler-TTS for native language audio (install via git)
# pip install git+https://github.com/huggingface/parler-tts.git
torch>=2.0.0  # Required by Parler-TTS
transformers>=4.35.0  # Required by Parler-TTS and medical models

# Image Processing (for OCR and medicine identification)
opencv-python>=4.8.0  # CRITICAL: For cv2 module
pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0  # For image processing algorithms

# OCR (pytesseract = Python bindings; install Tesseract engine from https://github.com/UB-Mannheim/tesseract/wiki)
pytesseract>=0.3.10
easyocr>=1.7.0

# Database & ORM
sqlalchemy>=2.0
alembic>=1.12
psycopg2-binary>=2.9.0  # PostgreSQL adapter for Python

# Authentication & Security
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0
PyJWT>=2.8.0

# Password hashing - using bcrypt directly (no passlib dependency)
bcrypt>=4.0.0

# Translation & Language Support
indic-trans2>=2.1.0  # For Indic language translation (9+ languages)
google-cloud-translate>=3.14.0  # Fallback translation API
langdetect>=1.0.9  # Language detection

# RAG & Vector Database for Medicine Knowledge Base
faiss-cpu>=1.7.4  # Vector similarity search for medicine knowledge base
sentence-transformers>=2.2.0  # Text embeddings for RAG
llama-index>=0.9.0  # RAG framework for medicine retrieval
langchain>=0.1.0  # Alternative RAG framework components

# Data Processing & Text Handling
pandas>=2.0.0
nltk>=3.8.1  # Natural language processing

# Ollama Integration (for Meditron LLM)
ollama>=0.0.1  # Ollama Python client
